defmodule DetectGithubUserProgrammingLanguagesTest do
  use ExUnit.Case
  doctest DetectGithubUserProgrammingLanguages

  test "greets the world" do
    assert DetectGithubUserProgrammingLanguages.hello() == :world
  end
end
